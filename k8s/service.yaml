apiVersion: v1
kind: Service
metadata:
  name: auth-next-service
spec:
  selector:
    app: auth-next
  ports:
  - port: 3000
    targetPort: 3000
  type: ClusterIP  # Use LoadBalancer or NodePort if external access needed
